// spawn every ten seconds at a random location and float down
// if they player colides with the vest they collect it
// every 15 seconds a ploice boat spawns and checks if the player has enough vests on board
// if not the player gets a fine = -5 seconds
